# Direct dependencies of weather_display.
#
# Transitive dependencies (aiohttp, certifi, etc.) are intentionally omitted;
# pin them in a lockfile if reproducible installs matter.
Pillow==10.4.0
requests==2.32.3

# --- optional: hardware-only (Raspberry Pi) ---------------------------------
# spidev and gpiozero are only importable on the Pi; install there separately:
#   pip install spidev gpiozero
